[package]
name = "listen_key_keepalive"
version = "0.1.0"
edition = "2024"

[dependencies]
binance = {path = "../../binance"}
tokio = { version = "1.49.0", features = ["full"] }

[features]
production = []